[project]
name = "banking-fastmcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.120.4",
    "fastmcp>=2.13.0.2",
    "py-key-value-aio>=0.2.8",
    "pydantic>=2.12.3",
    "python-dotenv>=1.2.1",
    "scalekit-sdk-python>=2.4.10",
    "starlette>=0.49.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
